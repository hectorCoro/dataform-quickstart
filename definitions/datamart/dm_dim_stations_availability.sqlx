config {
    type: "table",
    schema: constants.DM_SCH
}

SELECT
  station_id,
  date,
  SAFE_DIVIDE(SUM(docks_available), SUM(docks_available + bikes_available)) AS docks_availability
FROM ${ref("ods_bikeshare_status")}
GROUP BY station_id, date
ORDER BY station_id, date
