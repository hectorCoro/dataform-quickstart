config {
    type: "table",
    schema: constants.DM_SCH
}

SELECT
    start_station_id,
    CAST(`start_date` AS DATE) AS date,
    COUNT(start_station_id) AS num_arrives
FROM ${ref("ods_bikeshare_trips")}
GROUP BY start_station_id, date
