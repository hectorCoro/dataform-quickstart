config {
    type: "table",
    schema: constants.ODS_SCH
}

SELECT
  station_id,
  CAST(`time` AS DATE) AS date,
  SUM(bikes_available) AS bikes_available,
  SUM(docks_available) AS docks_available
FROM ${ref("st_bikeshare_status")}
GROUP BY station_id, date
